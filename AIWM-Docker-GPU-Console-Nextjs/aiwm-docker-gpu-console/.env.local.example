# Server-side only; backend origin without /api/v1.
AIWM_API_BASE_URL=http://localhost:8080
# Cookie Secure=true khi frontend HTTPS; false chỉ cho lab HTTP local.
AIWM_SESSION_COOKIE_SECURE=false
AIWM_API_TIMEOUT_MS=30000
NEXT_PUBLIC_REFRESH_INTERVAL_MS=5000
